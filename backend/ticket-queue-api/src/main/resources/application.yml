server:
  port: 18080

spring:
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
  application:
    name: ticket-queue-api
  data:
    redis:
      host: localhost
      port: 16379

logging:
  level:
    root: INFO
